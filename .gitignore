# colcon
build/
install/
log/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# transient run/setup logs (curated evidence lives in docs/media/*.txt)
*.log
docs/auto_resume_logs/

# overnight auto-resume tooling (local only, not part of the portfolio)
scripts/auto_resume.sh
.auto_resume_arm_epoch
.auto_resume.lock
.auto_resume_done

# editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# generated showcase and debug captures (large / transient)
docs/demo_showcase.html
docs/media/scene_check.png

# Artifacts from running the tests with uv locally. This package builds with
# colcon and its CI installs with pip, so a uv lock file is one tool leaking
# into a repository that does not use it.
uv.lock
.venv/
